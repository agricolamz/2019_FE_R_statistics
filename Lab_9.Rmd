---
title: "9-10. Основы фриквентистской статистики"
author: "Г. Мороз"
output:
  html_document:
    theme: spacelab
    highlight: pygments
    smooth_scroll: false
    df_print: paged
---

* [материалы занятия](https://github.com/agricolamz/2018_FE_R_statistics/raw/master/Lab%209-10.%20descriptive_and_simple_statistics.pdf)
* [домашнее задание](https://goo.gl/forms/BFgKk2EgXdQaUMi83)

Правило трех сигм:

```{r, message = FALSE, echo=  FALSE}
library(tidyverse)
ggplot(data.frame(x = 0:1), aes(x)) +
  stat_function(
    fun = dnorm,
    args = c(0, 1),
    geom = 'area',
    xlim = c(-3, 3),
    fill = 'deepskyblue4'
  ) +
  stat_function(
    fun = dnorm,
    args = c(0, 1),
    geom = 'area',
    xlim = c(-2, 2),
    fill = 'cadetblue') +
  stat_function(
    fun = dnorm,
    args = c(0, 1),
    geom = 'area',
    xlim = c(-1, 1),
    fill = 'lightblue') +
  stat_function(fun = dnorm,
                args = c(0, 1),
                xlim = c(-3, 3)) +
  theme_bw() +
  geom_line(aes(y = c(0.15), x = c(-1, 1)),
            arrow = arrow(
              length = unit(0.2, "cm"),
              ends = "both",
              type = "closed")) +
  geom_line(aes(y = c(0.03), x = c(-2, 2)),
            arrow = arrow(
              length = unit(0.2, "cm"),
              ends = "both",
              type = "closed"
            )) +
  annotate(geom = "text",
           x = 0,
           y = 0.17,
           label = "68.26%") +
  annotate(geom = "text",
           x = 0,
           y = 0.05,
           label = "95.44%") +
  scale_x_continuous(breaks = c(-3:3)) +
  labs(y = "",
       x = "σ")
```

Формула доверительного интервала:

$$\bar{x} \pm z \times \frac{\sigma}{\sqrt{n}}$$

Формула для  t-критерия:

$$t = \frac{\bar{x}-\mu_0}{\sigma/\sqrt{n}}$$
